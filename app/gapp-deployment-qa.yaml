apiVersion: apps/v1
kind: Deployment
metadata:
  name: gapp
  namespace: qa
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gapp
  template:
    metadata:
      labels:
        app: gapp
    spec:
      containers:
      - name: gapp
        image: phogatavi88/gradlew:v1.0.3
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: gapp-service
  namespace: qa
spec:
  type: NodePort
  selector:
    app: gapp
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30081
